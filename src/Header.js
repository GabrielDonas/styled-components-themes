import React from "react";
import { MainButton } from "./MainButton";

function Header() {
  return (
    <div>
      <MainButton>SlazSlaz</MainButton>
      <MainButton>SlazSlaz</MainButton>
    </div>
  );
}

export default Header;
